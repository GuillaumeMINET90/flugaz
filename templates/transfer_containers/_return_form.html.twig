{{ form_start(form) }}
<div class="form-container-global">
    <div>

        <div class="widget-container">
            <div class="form-widget">
                <label for="transfer_container_number" class="form-label inline-block text-gray-600">
                    N° de bouteille <i class ="font-bold text-red-600">*</i>
                </label>
                {{ form_row(form.number,{'disabled': true})}}    
            </div>
        </div>
        <div class="widget-container">
            <div class="form-widget">
                <label for="transfer_container_used_gaz" class="form-label inline-block text-gray-600">
                    Fournisseur
                </label>
                {{ form_row(form.vendor,{'disabled': true})}}    
            </div>
        </div>
        <div class="widget-container">
            <div class="form-widget">
                <label for="transfer_container_used_total_weight" class="form-label inline-block text-gray-600">
                    Date de retour <i class ="font-bold text-red-600">*</i>
                </label>
                {{ form_row(form.return_date)}} 
            </div>
        </div>


        {{ form_row(form._token) }}
        <div class="flex space-x-2 mt-3 justify-center">
            <div>
                <button type="button" onclick="window.location.href = '{{ path('app_transfer_containers_index') }}'" class="btn-return bg-blue-600 text-white"> 
                    Retour liste
                </button>

                <button type="submit" class="btn-green-validation bg-green-600 text-white">
                    Sauvegarder
                </button>
            </div>
        </div>
    </div>
</div>
  
{{ form_end(form) }}
