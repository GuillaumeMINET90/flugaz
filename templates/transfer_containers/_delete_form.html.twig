<form method="post" action="{{ path('app_transfer_containers_delete', {'id': transfer_container.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfer_container.id) }}">
    <button class="btn">Delete</button>
</form>
