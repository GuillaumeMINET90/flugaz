{% extends 'baseLogin.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<div class="container mx-auto mt-24 px-4">
    <div class="grid w-full justify-center">
    <div>
        <img class="mx-auto h-16 w-auto" src="{{ asset('images/icon_fg.png')}}" alt="Application FLUGAZ">
    </div>
        {% for flash_error in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}
        <h1 class="text-2xl text-center text-gray-800 font-bold m-4 ">
            Réinitialisez votre mot de passe
        </h1>
    </div>

    <p class="text-center">
        Un e-mail vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
        Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>
    <p class="text-center">If you don't receive an email please check your spam folder or <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 hover:text-blue-800 hover:underline">try again</a>.</p>


</div>


{% endblock %}
