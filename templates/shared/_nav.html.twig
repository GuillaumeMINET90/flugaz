<div class="relative">
    <div class="fixed sm:sticky top-0 left-0 right-0">
        <nav class="relative w-full flex flex-wrap items-center py-3 bg-blue-600 text-gray-500 hover:text-gray-700 focus:text-gray-700 shadow-lg">
            <div class="container-fluid w-full flex flex-wrap items-center  px-6">
                <div class="container-fluid w-full">

                    <button type="button" onclick="window.location.href = '{{ path('app_menu') }}'" class="float-left inline-block px-3 py-2 border-2 border-slate-50 text-slate-50 font-medium text-xs leading-tight uppercase rounded-full hover:bg-black hover:bg-opacity-5 focus:outline-none focus:ring-0 transition duration-150 ease-in-out">
                        <i class="text-white bi bi-house text-xl"></i>
                    </button>

                    <button type="button" onclick="history.back()" class="float-right px-3 py-2 border-2 border-slate-50 text-slate-50 font-medium text-xs leading-tight rounded-full hover:bg-black hover:bg-opacity-5 focus:outline-none focus:ring-0 transition duration-150 ease-in-out">
                        <i class="text-white bi bi-arrow-return-left text-xl"></i>
                    </button>
                </div>
            </div>
        </nav>
    </div>
</div>
