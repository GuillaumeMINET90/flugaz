<form method="post" action="{{ path('app_new_containers_movements_delete', {'id': new_containers_movement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ new_containers_movement.id) }}">
    <button class="btn">Delete</button>
</form>
