{% extends 'base.html.twig' %}

{% block title %}NewContainersMovements{% endblock %}

{% block body %}
    <h1>NewContainersMovements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ new_containers_movement.id }}</td>
            </tr>
            <tr>
                <th>Quantity_rest</th>
                <td>{{ new_containers_movement.quantityRest }}</td>
            </tr>
            <tr>
                <th>Quantity_injected</th>
                <td>{{ new_containers_movement.quantityInjected }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ new_containers_movement.date ? new_containers_movement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Cerfa_number</th>
                <td>{{ new_containers_movement.cerfaNumber }}</td>
            </tr>
            <tr>
                <th>Customer</th>
                <td>{{ new_containers_movement.customer }}</td>
            </tr>
            <tr>
                <th>Remark</th>
                <td>{{ new_containers_movement.remark }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_new_containers_movements_index') }}">back to list</a>

    <a href="{{ path('app_new_containers_movements_edit', {'id': new_containers_movement.id}) }}">edit</a>

    {{ include('new_containers_movements/_delete_form.html.twig') }}
{% endblock %}
