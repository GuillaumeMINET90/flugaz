<form method="post" action="{{ path('app_recovery_containers_movements_delete', {'id': recovery_containers_movement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recovery_containers_movement.id) }}">
    <button class="btn">Delete</button>
</form>
